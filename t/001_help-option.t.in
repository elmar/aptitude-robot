#!/usr/bin/env perl
use strict;
use warnings;
use 5.010;
use English qw( -no_match_vars );

use Test::More;

ok( system('perl', "@top_srcdir@/aptitude-robot", '--help') == 0,
    'option --help should be allowed' );

done_testing();
