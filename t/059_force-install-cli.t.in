#!/usr/bin/env perl
use strict;
use warnings;
use 5.010;
use English qw( -no_match_vars );

use Test::More;

use IPC::Run qw( run );

my $in = '';
my $out;
my $err;
my $cmd;

$cmd = [
    '@srcdir@/t/../aptitude-robot',
    '--config-dir=@srcdir@/t/testdata/force-install-1',
    '--force-install',
    '--show-cmdline',
];

ok(
    run( $cmd, \$in, \$out, \$err ),
    'should allow --force-install option on the command line',
);
is(
    $out,
    "aptitude -y full-upgrade bar- baz: foo+ quux=\n",
    'command line should not show hold and keep entries',
);

done_testing();
